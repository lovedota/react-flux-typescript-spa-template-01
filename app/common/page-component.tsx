// import * as React from "react";
// import PageService from "./page-services";

// export function createPageComponent(WrappedComponent, url, title, newStore) {
//     const pageData = PageService.getPageData(url, title, newStore);

//     return class extends React.Component<any, any> {
//         render() {
//             return (
//                 <WrappedComponent store={pageData.store} />
//             );
//         }
//     };
// };
